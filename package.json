{
  "name": "training-dogs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "sass": "sass --watch src/sass/main.scss:src/css/main.css",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^3.2.3"
  },
  "dependencies": {
    "sass": "^1.56.1"
  }
}
